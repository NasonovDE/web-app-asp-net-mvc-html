@using KinoAfisha.Models;
@model Film



<fieldset>
    <legend>Пожалуйста, заполните необходимые поля</legend>
    @using (Html.BeginForm("Edit", "Films", FormMethod.Post,
                  new { enctype = "multipart/form-data" }))
    {

        @Html.ValidationSummary();
        @Html.EditorForModel()
        <input type="submit" value="Сохранить" />
        <button>
            @Html.ActionLink("Отменить", "Index", "Films")
        </button>
    }
</fieldset>
