@using KinoAfisha.Models;
@model Kino

    <fieldset>
        <legend>Пожалуйста, заполните необходимые поля</legend>

        @using (Html.BeginForm("Create", "Kinos", FormMethod.Post, new { @enctype = "multipart/form-data" }))
        {
            @Html.EditorForModel()
            <input type="submit" value="Сохранить" />
            <button>  @Html.ActionLink("Отменить", "Index", "Kinos")</button>
        }
    </fieldset>