@using KinoAfisha.Models;
@model Kino

    <fieldset>
        <legend>Пожалуйста, заполните необходимые поля</legend>

        @using (Html.BeginForm("Edit", "Kinos", FormMethod.Post, new { @enctype = "multipart/form-data" }))
        {


            @Html.ValidationSummary();
            @Html.EditorForModel()
            <input type="submit" value="Сохранить" />
            <button>
                @Html.ActionLink("Отменить", "Index", "Kinos")
            </button>
        }
    </fieldset>