@using KinoAfisha.Models;
@model Format

    <fieldset>
        <legend>Пожалуйста, заполните необходимые поля</legend>
        @using (Html.BeginForm("Create", "Formats", FormMethod.Post))
        {

            @Html.EditorForModel()
            <input type="submit" value="Сохранить" />
            @Html.ActionLink("Отменить", "Index", "Formats")
        }
    </fieldset>