@using KinoAfisha.Models;
@model Description


    <fieldset>
        <legend>Пожалуйста, заполните необходимые поля</legend>
        @using (Html.BeginForm("Create", "Descriptions", FormMethod.Post))
        {

            @Html.EditorForModel()
            <input type="submit" value="Сохранить" />
            <button>
                @Html.ActionLink("Отменить", "Index", "Descriptions")
            </button>
        }
    </fieldset>
